{
  "name": "esolat-mcp-worker",
  "version": "2.0.0",
  "description": "esolat-mcp on Cloudflare Workers — MCP Streamable HTTP for Malaysian prayer times",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.5.3",
    "wrangler": "^3.91.0"
  }
}
